import React, { PureComponent } from "react";

export default class login extends PureComponent {
  render() {
    return (
      <div>
        Login
        <button>登录</button>
      </div>
    );
  }
}
