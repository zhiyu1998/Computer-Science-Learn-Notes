import React from 'react';
import ReactDOM from 'react-dom';
import Index from './04_styled-components/app';

ReactDOM.render(
  <React.StrictMode>
    <Index />
  </React.StrictMode>,
  document.getElementById('root')
);
